# Desktop Pet V2 0.1.1

本版本完善宠物动作系统、桌面交互体验和跨平台发布链路，并修复默认土豆点击
“睡觉”或“喂食”时动作不正确的问题。

## 主要更新

### 宠物动作与互动

- 为默认土豆加入完整动作旁车，提供 47 个动画，覆盖 30 个行为状态和 17 个运行时 cue。
- 完整实现哈欠、准备睡眠、睡眠、叫醒和待机的连续睡眠链，并加入明确的吃饭动画。
- 区分手动睡眠和自动低体力入睡：手动睡眠不会因体力充足立即自然醒。
- 状态面板现在会显示喂食、睡眠和互动的真实操作结果，包括饱食或冷却中的拒绝原因。
- Codex 宠物同步会保留动作旁车，同时继续保持导出包的基础文件兼容性。

### 界面与宠物资源

- 增加动作图集旁车运行时支持和动作完整的 `tudou_royal` 宠物包。
- 增加多套 Q 版应用图标和动态图标切换，并改进 macOS 原生单色托盘图标。
- 优化设置页、辅助面板、气泡和小游戏界面。

### 跨平台打包

- 完善 macOS、Windows、Linux 和 Microsoft Store 的构建与产物校验流程。
- 修复原生模块匹配、Linux 架构产物识别及跨平台 Release 元数据生成。
- 正式标签流水线会生成 SHA-256 校验、SPDX 2.3 SBOM 和统一 Release manifest。

## 验证

- 动作包严格校验：47 个动画，30/30 状态映射，17/17 cue，无错误或警告。
- `npm run check` 通过，包括 lint、类型检查、运行数据校验、125 项测试和生产构建。
- 动作接触表与睡眠链已通过独立视觉 QA。
